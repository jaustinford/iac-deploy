---
stages:
  - integrate
  - deploy

default:
  tags:
    - elysianskies_runner

variables:
  ELISE_CI_NEXUS_HOSTED: docker01-teine.home.elysianskies.com:32501
  ELISE_CI_GITLAB_ENDPOINT: docker02-teine.home.elysianskies.com:32512
  TF_VAR_linode_token: $ELISE_PROJECT_LINODE_TOKEN

include:
  - local: ci/build.yml
  - local: ci/manual.yml
  - local: ci/scheduled.yml
